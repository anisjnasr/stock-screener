# Massive (formerly Polygon.io) — get your API key at https://polygon.io
# Copy this file to .env.local and add your key:
# cp .env.example .env.local
MASSIVE_API_KEY=your_massive_api_key_here

# Supabase — profile sync (cross-device watchlists, scans, settings)
# Create a free project at https://supabase.com, then run data/supabase-schema.sql
# in the SQL Editor and copy the URL + anon key from Settings > API.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only: cron routes (economic calendar + market events). From Supabase Settings > API > service_role.
SUPABASE_SERVICE_ROLE_KEY=
# Bearer secret for cron routes (may match ADMIN_SECRET if you use one secret on Render).
# Used by POST /api/cron/economic-calendar, POST /api/cron/market-events/*, earnings crons,
# POST /api/cron/newsletter-ingest, and POST /api/cron/theme-extraction.
CRON_SECRET=

# Anthropic — AI completion, theme extraction cron,
# and Large Cap Analysis synthesis on the Python service (POST /large-cap/synthesize). Key is server-only.
# ANTHROPIC_API_KEY=
# Optional default model for Large Cap only (Python). Default claude-sonnet-4-6; e.g. claude-opus-4-7
# ANTHROPIC_LARGE_CAP_MODEL=

# Small-Cap Due Diligence panel (premarket page).
# Run data/supabase-small-cap-dd.sql in the Supabase SQL editor to create dd_overrides / dd_reports.
# EDGAR requires a contact User-Agent on every request (falls back to SEC_USER_AGENT, then a placeholder):
# EDGAR_USER_AGENT="StockStalker you@email"
# Optional model overrides (defaults shown): dilution extraction + news relevance classification.
# DD_EXTRACTION_MODEL=claude-sonnet-4-6
# DD_NEWS_MODEL=claude-haiku-4-5

# Large Cap cache (Python service): same Supabase project as Next.js — service role for large_cap_analysis_cache
# SUPABASE_URL=   (or reuse NEXT_PUBLIC_SUPABASE_URL value)
# SUPABASE_SERVICE_ROLE_KEY=

# Phase 4 — Gmail newsletter ingest for themed premarket context
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GMAIL_REFRESH_TOKEN=
# Optional: comma-separated allowlist override (skips reading config/newsletters-for-writeups.txt)
# NEWSLETTER_ALLOWLIST=crew@morningbrew.com,doomberg@substack.com

# Phase 12A/B — Python microservice on Render (yfinance /news). PYTHON_SERVICE_KEY must match Python INTERNAL_API_KEY.
# PYTHON_SERVICE_URL=https://your-python-service.onrender.com
# PYTHON_SERVICE_KEY=

# Finnhub — earnings calendar (Phase 9). https://finnhub.io/register
# Used by POST /api/cron/earnings/ingest and POST /api/cron/earnings/actuals.
FINNHUB_API_KEY=

# Optional: TradingView scanner cookies (server-only) for richer gapper scan data
# used as input to the THEMES extraction cron.
# TRADINGVIEW_SESSIONID=
# TRADINGVIEW_SESSIONID_SIGN=

# Optional: gapper scan timing overrides (server-only) for the THEMES gapper input.
# Server default min premarket volume when scan omits minPmVolume.
# PREMARKET_MIN_PM_VOLUME_DEFAULT=25000
# Weekday ET early-window guard (HH:MM) and floor volume used to suppress stale carryover rows.
# PREMARKET_EARLY_ET_START_HHMM=04:00
# PREMARKET_EARLY_ET_END_HHMM=05:30
# PREMARKET_EARLY_MIN_PM_VOLUME=25000

# Optional: admin bulk/manual routes (sync DB, import bars, manual newsletter ingest from UI).
# ADMIN_SECRET=
